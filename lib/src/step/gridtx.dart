// how are we going to manage the generation of ids.
import 'cell.dart';
import 'db.dart';
import 'grid.dart';
import './gen/data.pb.dart';

class GridTx extends DocTx {
  // Tx tx;
  // Grid grid;
  // List<GridStep> step = [];
  // GridTx(this.tx, this.grid) {
  //   tx.add(this);
  // }

  // void insert(int dim, int before, int count) {}

  // void remove(int dim, int starting, int count) {
  //   SequenceStep st = step.dim[dim];
  //   st.remove
  //     ..add(starting)
  //     ..add(count);
  // }

  // // we don't know the id's yet, maybe not even in a local sense?
  // // image the database in an isolate.
  // void update(CellStep update) {
  //   //step.cell.add(update);
  // }
}
